{
  "name": "gscript-seth",
  "version": "1.0.0",
  "description": "Google Apps Script project",
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
